{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git commit*)",
            "command": "claude --print \"Review CLAUDE.md against the current codebase. If architecture, commands, modules, config options, API routes, or workflow have changed since CLAUDE.md was last written, update it in place. Only change sections that are outdated or missing — do not rewrite sections that are still accurate.\"",
            "timeout": 120,
            "statusMessage": "Updating CLAUDE.md..."
          }
        ]
      }
    ]
  }
}
