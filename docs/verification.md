# ATLAS Rebuild — Verification Evidence

> Required by `atlas_rebuild_desc.md` §4.3. Tracks evidence, not opinions. No requirement may be marked `verified` without concrete evidence.

**Statuses:** `not-started` | `in-progress` | `verified` | `failed` | `blocked` | `needs-recheck`

**Last updated:** 2026-06-12

**Final stamp (2026-06-12, end of rebuild session):** `compileall` clean → `ruff` All checks passed → pytest **152 passed** → `run_benchmark.py` **BENCHMARK PASSED** (17/17), single chained command, exit 0.

| Requirement | Evidence | Command/File | Result | Status |
| --- | --- | --- | --- | --- |
| Tracking files exist | 7 files created in `docs/` | `docs/{progress,handoff,verification,decision-log,critique,risk-register,phase-gates}.md` | created 2026-06-11 | verified |
| GitNexus index fresh | analyze run | `npx.cmd gitnexus analyze` | exit 0 (2026-06-11) | verified |
| Tests attempted (Phase 1) | full suite run twice | `.venv\Scripts\python -m pytest` | 1st run: 76 passed / 1 failed (`test_generator.py` reference-title regression); after fix in `src/agents/generator.py::_merge_sources`: **77 passed** | verified |
| Lint clean (Phase 1) | ruff run | `.venv\Scripts\python -m ruff check src tests main.py` | All checks passed, exit 0 | verified |
| Compile clean (Phase 1) | compileall run | `.venv\Scripts\python -m compileall -q src main.py` | exit 0 | verified |
| Runtime attempted (Phase 1) | app booted locally | `.venv\Scripts\python main.py`; `Invoke-WebRequest /health, /, /api/history` | `/health` 200 `{"status":"ok","service":"ATLAS"}`; `/` 200 (7013 bytes); `/api/history` 200 valid UTF-8 JSON | verified |
| Encoding check (Phase 1) | mojibake grep across py/html/js/css/md/yaml/json | grep `á»\|Ã¡\|Ä‘\|áº` | No matches; API JSON decodes correctly as UTF-8 | verified |
| Audit exists | audit written from verified evidence | `docs/audit.md` | complete (architecture, works/broken, debt, gaps, strategy) | verified |
| PRD exists | — | `docs/prd.md` | — | not-started |
| Architecture doc exists | — | `docs/architecture.md` | — | not-started |
| Chat modes distinct (UI+backend) | Mode registry + profiles + distinct templates/search policies; UI selector updated; tests `test_modes.py` (9 tests incl. distinct-behavior + profile scaling) | `.venv\Scripts\python -m pytest tests/test_modes.py` | 99/99 suite green 2026-06-11 | verified |
| Citations generated & linked | `_ensure_report_structure` links `[[N]](#source-N)` + rebuilds `## Sources` from context URLs; 7 generator tests | `.venv\Scripts\python -m pytest tests/test_generator.py` | passing (EN headings) | verified |
| Non-AI refusal behavior | `scope_gate` entry node (keyword fast-path + LLM classify, fail-open), refusal WS message + report; routing test | `.venv\Scripts\python -m pytest tests/test_scope_gate.py` | 9 tests passing | verified |
| Full suite green after Phase 5+6 code | compile + ruff + pytest in one run | `.venv\Scripts\python -m compileall -q src main.py && ruff check && pytest -q` | All checks passed; **137 passed** in 35.95s (2026-06-12) | verified |
| Source quality scoring | 9-category deterministic scorer wired into searcher/context/report; 13 tests (classification per category, ranking, low-quality exclusion + flagged fallback, category labels in references) | `src/quality/source_scorer.py`; `.venv\Scripts\python -m pytest tests/test_source_scorer.py` | 13/13 passing (2026-06-12, part of 137) | verified |
| Sample output: ranking + citations | Deterministic sample generated by real pipeline code (scorer → context builder → report normalizer); shows ranking table, low-quality exclusion, `[[N]](#source-N)` anchors, category labels | `.venv\Scripts\python scripts\make_sample_report.py` → `docs/samples/research-mode-sample.md` | generated 2026-06-12 | verified |
| Research system documented | Pipeline, taxonomy, low-quality policy, citation system, D-009 research rationale + baseline comparison, honest limitations | `docs/research-system.md` | written 2026-06-12, matches code behavior | verified |
| Stale code removed (Phase 6) | superseded academic filters + orphan test deleted; GitNexus impact LOW; package `__init__` updated | `git status` D: `src/quality/academic_filter.py`, `src/utils/academic_filter.py`, `tests/test_utils.py` | suite after removal **132 passed**; ruff + compileall clean (incl. `scripts/`) 2026-06-12 | verified |
| Automation configurable | REST config GET/PUT with Pydantic validation (tz via ZoneInfo, HH:MM, email regex, depth whitelist); single-row SQLite config | `src/api/routes/automation.py`, `src/automation/store.py`; runtime smoke: GET/PUT `/api/automation/config` 200 (2026-06-11) | tests + runtime 200s | verified |
| Daily report generation | `run_daily_report` builds 13-section daily query, runs researcher, saves history `kind="daily"`; real scheduled run completed: run `7bf711e9` status=success, history_id `8b5a5015` | `src/automation/daily_report.py`; `GET /api/automation/runs` (2026-06-11) | success against live OpenAI+Tavily | verified |
| Email send / dev mock fallback | `EmailSettings.from_env()` auto-detects mock vs smtp; retry (1s/2s/4s); real run recorded `email_status="mocked"` | `src/automation/email_sender.py`; run `7bf711e9` | mock path verified at runtime; real SMTP untested (no creds) — documented limitation | verified |
| Run history (automation) | `automation_runs` table + GET `/api/automation/runs[/{id}]`; stale-running marked failed on startup | `src/automation/store.py`, `src/api/app.py` lifespan | tests + runtime list 200 | verified |
| Scheduler/email tests | 25 tests: is_due/tz/HH:MM parsing, idempotency (mark_attempted), tick_once, no-send guards, email render/retry/mock, API validation | `.venv\Scripts\python -m pytest tests/test_automation.py` | 25/25 passing (part of 137) | verified |
| Evaluation command runs (online) | full pipeline + judge + in-workflow eval | `.venv\Scripts\python run_eval.py quick` | **Overall 0.9750 [PASS]**: context_relevance 0.9, faithfulness 1.0, answer_relevance 0.9, citation_coverage 1.0, refusal/over-answering/noise/scope all pass; `vietnamese_quality_check=skipped` for EN query (rubric fix verified); RAGAS adapter skipped (informational). 2026-06-12 | verified |
| Offline benchmark runs | deterministic harness, exit code gated | `.venv\Scripts\python run_benchmark.py` | 5/5 samples, **17/17 metric expectations**, exit 0 (incl. hallucination/over-answering caught, bilingual not punished). 2026-06-12 | verified |
| Eval metric unit tests | offline tests for ranking/refusal/grounding/citation/aggregation | `.venv\Scripts\python -m pytest tests/test_evaluation_metrics.py` | 20/20 passing (suite total **152**) | verified |
| Evaluation documented | metric set + thresholds + commands + D-009 rationale + honest limits | `docs/evaluation.md` | written 2026-06-12, matches code | verified |
| UI product-first (app shell) | Full frontend rebuild: topbar nav (Research/Automation/History), mode cards, progress card w/ stage + log stream, report card, sources panel, follow-ups, automation settings + runs, history grid w/ kind filter | browser verification via preview tooling: screenshots of all 3 views, 2026-06-12 | all views render, navigation works, no console errors | verified |
| Live chat run through UI | Real quick-mode run from the browser: busy form → progress stages → streamed report → 8 ranked sources w/ category chips (Peer-reviewed 90/100, arXiv 80/100) → follow-up questions → Copy/PDF enabled → saved to history | browser run "speculative decoding" query, 2026-06-12 | end-to-end success | verified |
| Refusal state in UI | Out-of-scope queries ("chocolate cake recipe", "workout plan") show amber refusal card w/ reframe suggestion; result area stays hidden; form recovers | browser runs 2026-06-12 (duplicate-report bug found and fixed during verification) | verified after fix | verified |
| Automation UI real & wired | Config loaded from GET; PUT save → "Settings saved."; invalid timezone → "unknown IANA timezone" error shown; mock-email chip honest; runs list shows real scheduled run (SUCCESS / EMAIL: MOCKED / 44s) + working "View report" | browser interaction 2026-06-12 | verified |
| History UI (chat + daily) | Kind filter All/Chat/Daily works (after fixing kind="daily_report" mismatch); badges correct; stored daily report opens and renders w/ clickable citations; delete/export/clear-all wired to real endpoints | browser interaction 2026-06-12 | verified |
| Responsive | mobile 375×812: stacked mode picker, full-width button, no overflow; desktop 1280: two-column report+sources grid | preview_resize screenshots 2026-06-12 | verified |
| Encoding correctness (UI) | Legacy Vietnamese history entry renders correctly in EN UI; no mojibake in any view | browser screenshot 2026-06-12 | verified |
| `.env.example` complete | all runtime env vars documented incl. auth, CORS, caches, eval, SMTP/email-mode section | `.env.example` | reviewed 2026-06-12, matches code (`EmailSettings.from_env`, config.py, app.py) | verified |
| No hardcoded secrets | regex sweep for sk-/tvly-/AIza key shapes; `.env` gitignored; SMTP creds never in DB or API responses | Grep scan 2026-06-12; `_public_config` exposes only derived `email_mode` | no matches | verified |
| Auth enforced | bearer + query-token auth on /api/* and /ws; disabled when token unset (documented as local-dev only) | `tests/test_server_auth.py` (part of 152) | passing | verified |
| Docker/deploy configs match app | prod compose: added EMAIL_MODE/SMTP_*/CORS_ORIGINS passthrough; dev compose rewritten (stale `./app` mount, `history.json`, FLASK_ENV removed; correct volumes); healthchecks → `/health` | `docker compose -f <file> config --quiet` | both valid 2026-06-12; full prod image build NOT re-run (R-09, documented in deployment.md §8) | verified |
| Security & deployment docs | threat model, auth, secrets, validation, exposure, cost controls, honest gaps table; deploy guide w/ single-replica constraint (D-005), backups, proxy config | `docs/security.md`, `docs/deployment.md` | written 2026-06-12, match code behavior | verified |
| README accurate | rewritten for rebuilt product: modes quick/research/deep, trust system, automation, eval commands, API surface, docs links, honest limitations | `README.md` | reviewed against code 2026-06-12 | verified |
| Docs complete (launch set) | product, competitive-analysis, prd, architecture, research-system, evaluation, automation, security, deployment, user-guide, roadmap, launch-checklist, audit + 7 tracking files | `docs/*.md` | all present 2026-06-12 | verified |
| Sample outputs exist | real unedited live run (Quick Answer, captured from history API) + deterministic regenerable pipeline sample with ranking table | `docs/samples/quick-answer-live-sample.md`, `docs/samples/research-mode-sample.md` | both present 2026-06-12 | verified |
| Launch checklist | item-by-item status with caveats (R-09 docker build, SMTP untested, small benchmark) | `docs/launch-checklist.md` | written 2026-06-12 | verified |
